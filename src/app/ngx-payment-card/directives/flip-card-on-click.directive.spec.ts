import { FlipCardOnClickDirective } from './flip-card-on-click.directive';

describe('FlipCardOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipCardOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
